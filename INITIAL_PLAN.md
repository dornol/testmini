# QA Management System (Internal TMS)

## 1. Overview

Internal QA management system for handling test cases and execution
results per project.

Goals:

- Project isolation
- Test case versioning
- Release-based test runs
- Detailed failure tracking
- File attachments
- Multi-user collaboration
- Conflict prevention (Soft Lock + Optimistic Lock)
- Real-time execution sync (WebSocket)
- Multi-language support (ko / en)

Automation integration will be introduced in Phase 2.

---

## 2. Tech Stack

### Frontend + Backend (Monolith)

- Svelte 5
- SvelteKit (SSR + API)
- TypeScript
- TailwindCSS v4 (plugins: forms, typography)
- Paraglide (i18n, ko / en)

### Database & ORM

- PostgreSQL
- Drizzle ORM (postgres-js driver)

### Authentication

- better-auth (email/password + OIDC)
- DB-based session (better-auth default)

### Cache / Lock / PubSub (Phase 2+)

- Redis (Soft Lock, Pub/Sub)

### File Storage (Phase 1+)

- S3-compatible storage (MinIO)

### Real-time (Phase 2+)

- WebSocket (Run screen synchronization)

### Testing

- Vitest (unit/server tests)
- Playwright (browser/component tests)

---

## 3. Architecture

```
Svelte 5 UI
    |
SvelteKit (SSR + API + WS)
    |
  +-----------+-----------+
  |           |           |
PostgreSQL  Redis(P2)  MinIO
```

- Single monolithic service
- API + SSR integrated
- Redis-based locking and event broadcasting (Phase 2+)
- Docker Compose for local development
- `adapter-auto` for deployment (configurable per environment)

### Docker Compose Services

| Service    | Phase   | Description                  |
| ---------- | ------- | ---------------------------- |
| PostgreSQL | Phase 1 | Primary database             |
| Redis      | Phase 2 | Soft Lock, Pub/Sub           |
| MinIO      | Phase 1 | S3-compatible file storage   |

---

## 4. Authentication

### Provider: better-auth

Already configured with:

- `betterAuth` with drizzle adapter (`provider: 'pg'`)
- `emailAndPassword: { enabled: true }`
- `sveltekitCookies` plugin for SvelteKit integration
- Session available via `event.locals.user` / `event.locals.session`

### Login Types

- Local account (email / password) — Phase 1
- OIDC providers — Phase 1+ (better-auth plugin)

### Session Strategy

- httpOnly cookie (better-auth default)
- DB-based session storage (better-auth manages `session` table)
- No Redis dependency for sessions

### Auth Schema

Generated by `better-auth generate` command:

```bash
pnpm auth:schema
```

Tables managed by better-auth:

- `user` — id, email, name, emailVerified, image, createdAt, updatedAt
- `session` — id, userId, expiresAt, token, ipAddress, userAgent
- `account` — id, userId, providerId, providerAccountId, ...
- `verification` — id, identifier, value, expiresAt

> Custom auth tables (User, UserAuth) are NOT needed.
> better-auth handles all auth-related schema automatically.

---

## 5. Authorization

### Global Role

- ADMIN — system-wide management
- USER — default role

### Project Role

- PROJECT_ADMIN — project settings, member management
- QA — create/edit test cases, execute runs
- DEV — view test cases, execute runs
- VIEWER — read-only access

### ProjectMember

| Column     | Type      | Description       |
| ---------- | --------- | ----------------- |
| id         | serial    | PK                |
| project_id | integer   | FK → Project      |
| user_id    | text      | FK → user (better-auth) |
| role       | enum      | Project Role      |
| created_at | timestamp | Join date         |

---

## 6. Domain Model

### Project

| Column      | Type      | Description        |
| ----------- | --------- | ------------------ |
| id          | serial    | PK                 |
| name        | text      | Project name       |
| description | text      | Optional           |
| active      | boolean   | Soft delete flag   |
| created_by  | text      | FK → user          |
| created_at  | timestamp | Creation timestamp |

### TestCase

| Column            | Type      | Description             |
| ----------------- | --------- | ----------------------- |
| id                | serial    | PK                      |
| project_id        | integer   | FK → Project            |
| key               | text      | Display key (TC-0001)   |
| latest_version_id | integer   | FK → TestCaseVersion    |
| created_by        | text      | FK → user               |
| created_at        | timestamp | Creation timestamp      |

### TestCaseVersion

| Column          | Type      | Description                |
| --------------- | --------- | -------------------------- |
| id              | serial    | PK                         |
| test_case_id    | integer   | FK → TestCase              |
| version_no      | integer   | Sequential version number  |
| title           | text      | Test case title            |
| precondition    | text      | Prerequisites              |
| steps           | jsonb     | Ordered test steps (below) |
| expected_result | text      | Overall expected result    |
| priority        | enum      | LOW / MEDIUM / HIGH / CRITICAL |
| revision        | integer   | Optimistic lock counter    |
| updated_by      | text      | FK → user                  |
| created_at      | timestamp | Version creation timestamp |

**Steps JSONB Schema:**

```typescript
type TestStep = {
  order: number;       // Step sequence (1, 2, 3...)
  action: string;      // Action to perform
  expected: string;    // Expected result for this step
};
// Column type: TestStep[]
```

New row is created on each update. `revision` field is used for
optimistic locking within the same version.

### TestRun

| Column      | Type      | Description                          |
| ----------- | --------- | ------------------------------------ |
| id          | serial    | PK                                   |
| project_id  | integer   | FK → Project                         |
| name        | text      | Run name (e.g., v1.2.0 QA)          |
| environment | enum      | DEV / QA / STAGE / PROD              |
| status      | enum      | CREATED / IN_PROGRESS / COMPLETED    |
| started_at  | timestamp | Execution start                      |
| finished_at | timestamp | Execution end                        |
| created_by  | text      | FK → user                            |

### TestExecution

| Column                 | Type      | Description                   |
| ---------------------- | --------- | ----------------------------- |
| id                     | serial    | PK                            |
| test_run_id            | integer   | FK → TestRun                  |
| test_case_version_id   | integer   | FK → TestCaseVersion          |
| status                 | enum      | PASS / FAIL / BLOCKED / SKIPPED / PENDING |
| comment                | text      | Optional note (any status)    |
| executed_by            | text      | FK → user                     |
| executed_at            | timestamp | Execution timestamp           |

### TestFailureDetail

| Column              | Type      | Description              |
| ------------------- | --------- | ------------------------ |
| id                  | serial    | PK                       |
| test_execution_id   | integer   | FK → TestExecution       |
| failure_environment | text      | Environment details      |
| test_method         | text      | Method/approach used     |
| error_message       | text      | Error message            |
| stack_trace         | text      | Stack trace              |
| comment             | text      | Additional notes         |
| created_by          | text      | FK → user                |
| created_at          | timestamp | Creation timestamp       |

### Attachment

| Column         | Type      | Description                              |
| -------------- | --------- | ---------------------------------------- |
| id             | serial    | PK                                       |
| reference_type | enum      | TESTCASE / EXECUTION / FAILURE           |
| reference_id   | integer   | Polymorphic FK (no DB constraint)        |
| file_name      | text      | Original file name                       |
| content_type   | text      | MIME type                                |
| object_key     | text      | S3 object key                            |
| file_size      | integer   | File size in bytes                       |
| uploaded_by    | text      | FK → user                                |
| uploaded_at    | timestamp | Upload timestamp                         |

> Polymorphic reference — FK constraint is not enforced at DB level.
> Application-level validation ensures referential integrity.

**Upload flow:**

1. Client requests presigned URL from API
2. Client uploads file directly to MinIO via presigned URL
3. Client confirms upload, API saves metadata to `attachment` table

---

## 7. Concurrency Strategy

### TestCase Editing (Phase 2 — requires Redis)

**Soft Lock (Redis):**

- Key: `lock:testcase:{testCaseVersionId}`
- Value: `userId`
- TTL: 10 minutes

Flow:

1. Create lock when editing starts
2. Deny editing if lock exists (show lock holder info)
3. Release on save / cancel
4. Auto-expire by TTL

**Optimistic Lock (Phase 1 — no Redis needed):**

```sql
UPDATE test_case_version
SET ..., revision = revision + 1
WHERE id = ? AND revision = ?
```

If 0 rows affected → conflict detected → notify user.

### TestRun Execution Screen (Phase 2)

1. Save execution result
2. Broadcast WebSocket event via Redis Pub/Sub
3. Update only modified row on other clients
4. Use optimistic lock for integrity

---

## 8. WebSocket Design (Phase 2)

Endpoint: `/ws/run/{runId}`

Event Types:

```typescript
type WsEvent =
  | { type: 'EXECUTION_UPDATED'; executionId: number; status: string; updatedBy: string; updatedAt: string }
  | { type: 'RUN_STATUS_CHANGED'; runId: number; status: string }
  | { type: 'USER_JOINED'; userId: string }
  | { type: 'USER_LEFT'; userId: string };
```

Redis Pub/Sub used for multi-instance scalability.

---

## 9. Internationalization (i18n)

### Provider: Paraglide

- Languages: Korean (ko), English (en)
- Message files: `messages/ko.json`, `messages/en.json`
- Runtime: Paraglide Vite plugin + SvelteKit middleware
- Direction: LTR (both languages)

### Scope

- All UI labels, buttons, messages
- Validation error messages
- Navigation and menu items

---

## 10. Key Screens

- **Project list & management** — CRUD, member management
- **Test case list** — filter + search + latest status, bulk actions
- **Test case detail** — version history, edit with lock indicator
- **Test run creation** — select test cases, set environment
- **Run execution screen** — bulk pass, fail modal, real-time stats (Phase 2: WebSocket)
- **Dashboard** — pass rate, failure rate, execution metrics

---

## 11. Performance Considerations

- Mandatory server-side pagination (cursor or offset)
- Database indexes:
  - `test_case(project_id)`
  - `test_case_version(test_case_id, version_no DESC)`
  - `test_execution(test_run_id, status)`
  - `attachment(reference_type, reference_id)`
- Virtual scrolling for run execution screen
- GIN index on `steps` JSONB if search is needed

---

## 12. Testing Strategy

### Unit Tests (Vitest, node environment)

- Server-side logic, utilities, validation
- File pattern: `src/**/*.{test,spec}.{js,ts}`

### Component Tests (Vitest + Playwright)

- Svelte component rendering and interaction
- File pattern: `src/**/*.svelte.{test,spec}.{js,ts}`
- Runs in headless Chromium

### Configuration

- `requireAssertions: true` — every test must have at least one assertion
- Server tests excluded from browser runner
- Component tests excluded from server runner

---

## 13. Development Phases

### Phase 1 (MVP)

- [x] Project scaffolding (SvelteKit, Drizzle, TailwindCSS, Paraglide)
- [x] Auth setup (better-auth, email/password)
- [ ] Auth schema generation and OIDC provider
- [ ] Project CRUD + member management
- [ ] TestCase + TestCaseVersion (optimistic lock only)
- [ ] TestRun + TestExecution
- [ ] TestFailureDetail
- [ ] Attachment (MinIO presigned upload)
- [ ] i18n for all UI (ko / en)

### Phase 2

- [ ] Redis integration (Docker Compose)
- [ ] Soft Lock for test case editing
- [ ] WebSocket real-time sync for run screen
- [ ] Basic dashboard (pass rate, metrics)

### Phase 3

- [ ] Advanced search (full-text, filters)
- [ ] Performance optimization (virtual scrolling, query tuning)
- [ ] Bulk operations (import/export test cases)

### Phase 4 (Automation)

- [ ] `automation_key` field on TestCase
- [ ] Automation result ingestion API
- [ ] CI integration (webhook / polling)

---

## 14. Future Extensions

- CI/CD integration (GitHub Actions, GitLab CI)
- Failure trend analytics
- Slack / webhook notifications
- Flaky test detection
- Project templates
- Test case tagging and categorization
- Audit log